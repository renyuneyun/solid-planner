/**
 * @deprecated This file is deprecated. Use solid-service.ts instead.
 * All Solid Pod operations have been migrated to the new SolidTaskService.
 * 
 * Example usage:
 * ```typescript
 * import { createSolidTaskService } from '@/utils/solid-service'
 * 
 * const service = createSolidTaskService(podRootUri, authFetch)
 * await service.fetchTasks()
 * await service.saveTaskClasses(taskClasses)
 * ```
 */
