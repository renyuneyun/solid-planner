/**
 * Animation configuration for the application.
 * Update these values to easily change animation timings across the app.
 */

export const ANIMATIONS = {
  // Duration for the completion fade-out animation (in milliseconds)
  COMPLETION_FADE_DURATION: 3000,
} as const
